// Template. Copy this file to config.js (same folder as index.html) and fill in
// your Apps Script Web App URL. config.js is gitignored on purpose — the URL is a
// bearer secret (full read/write to your Sheet, no login), so it must never be
// committed or shipped in a public build.
window.CONFIG = {
  SCRIPT_URL: 'PASTE_YOUR_APPS_SCRIPT_WEBAPP_/exec_URL_HERE'
};
